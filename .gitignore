# 敏感配置（含 API Key，绝不能推到公网）
config.json

# 运行时生成的数据
data/
qrcode.png

# 教师自己录入的题目包（评委看代码不需要这些）
questions/

# Python 缓存
__pycache__/
*.pyc
*.pyo

# PyInstaller 构建产物（太大，不放源码仓库；要给评委可用的 exe 就用 GitHub Release 上传）
build/
dist/
*.spec
